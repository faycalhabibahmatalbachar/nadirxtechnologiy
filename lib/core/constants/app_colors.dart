import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // === FONDS ===
  static const Color background = Color(0xFF050508);
  static const Color surface = Color(0xFF0D0D14);
  static const Color surfaceVariant = Color(0xFF12121C);
  static const Color surfaceElevated = Color(0xFF1A1A28);

  // === ACCENT PRINCIPAL — Vert terminal ===
  static const Color primary = Color(0xFF00FF88);
  static const Color primaryDim = Color(0xFF00CC6A);
  static const Color primaryGlow = Color(0x2200FF88);

  // === ACCENT SECONDAIRE — Bleu électrique ===
  static const Color secondary = Color(0xFF00D4FF);
  static const Color secondaryDim = Color(0xFF0099BB);
  static const Color secondaryGlow = Color(0x2200D4FF);

  // === ACCENT TERTIAIRE — Violet cyber ===
  static const Color tertiary = Color(0xFF8B5CF6);
  static const Color tertiaryGlow = Color(0x228B5CF6);

  // === ÉTATS ===
  static const Color success = Color(0xFF00FF88);
  static const Color error = Color(0xFFFF3B5C);
  static const Color warning = Color(0xFFFFB800);
  static const Color info = Color(0xFF00D4FF);

  // === TEXTES ===
  static const Color textPrimary = Color(0xFFE8E8F0);
  static const Color textSecondary = Color(0xFF8888AA);
  static const Color textTertiary = Color(0xFF44445A);
  static const Color textAccent = Color(0xFF00FF88);

  // === BORDURES ===
  static const Color border = Color(0xFF1E1E30);
  static const Color borderFocus = Color(0xFF00FF88);
  static const Color borderGlow = Color(0x4400FF88);

  // === SPÉCIAUX ===
  static const Color scanline = Color(0x0500FF88);
  static const Color gridLine = Color(0x08FFFFFF);
}
